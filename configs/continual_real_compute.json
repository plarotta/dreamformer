{
  "run_name": "continual_real_compute",
  "variant": "full_prioritized",
  "seed": 22,
  "device": "cuda",
  "output_dir": "artifacts/runs",
  "model_overrides": {
    "vocab_size": 256,
    "max_seq_len": 256,
    "d_model": 256,
    "n_heads": 8,
    "n_layers": 8,
    "ffn_dim": 512,
    "memory_layer_index": 6,
    "num_stm_slots": 512,
    "memory_key_dim": 128,
    "memory_value_dim": 128,
    "replay_capacity": 60000
  },
  "train_overrides": {
    "steps": 1000,
    "batch_size": 96,
    "seq_len": 192,
    "learning_rate": 0.0003,
    "weight_decay": 0.01,
    "grad_clip_norm": 1.0,
    "log_every": 50,
    "eval_every": 500,
    "checkpoint_every": 1000,
    "nrem_every": 10,
    "eval_batches": 64,
    "amp": true
  },
  "phases": [
    { "task": "passkey", "steps": 8000 },
    { "task": "needle", "steps": 8000 },
    { "task": "passkey", "steps": 8000 }
  ]
}
